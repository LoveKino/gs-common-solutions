{
  "name": "{{.projectName}}",
  "version": "0.0.1",
  "description": "{{.projectDescription}}",
  "main": "index.js",
  "scripts": {
    "build": "webpack",
    "watch": "webpack --watch",
    "test": "mocha test/**/*.js",
    "lint": "eslint src/**/*.js",
    "dev": "open http://127.0.0.1:3000; python -m SimpleHTTPServer 3000"
  },
  "keywords": [],
  "author": "{{.author}}",
  "license": "MIT",
  "devDependencies": {
  },
  "dependencies": {
  }
}
